# 生产环境：后端 API 基址。
# 正式版后端：http://134.175.77.51:9292
#
# ⚠️ 直连该地址（与前端不同源）需注意两点：
#   1) CORS：后端须放行前端来源（跨端需求，见 docs/前台-*.md，由后端配置）。
#   2) 混合内容：若前端站点用 HTTPS，浏览器会拦截对 http:// 后端的请求（mixed content）。
#      → 前端也用 HTTP，或给后端上 HTTPS，或改用「同源反向代理」（推荐）。
#
# 【推荐】把本值留空 + Nginx 反向代理：前端与 API 同源，免 CORS、免混合内容。
# 反代配置见 docs/部署-宝塔完整部署教程.md §8（location 反代 /tool /user /query 等到后端）。
REACT_APP_API_BASE=http://134.175.77.51:9292
