# 复制本文件为 .env.local 并按需修改（.env.local 已被 gitignore）

# 是否启用内置 mock。默认 true（开箱即跑，不需要后端）。
# 接真实后端时设为 false。
VITE_USE_MOCK=true

# 真实后端基址（仅 VITE_USE_MOCK=false 时生效）。
# 可写后端完整地址（如 http://localhost:8080/api），
# 或保持 /api 配合 vite.config.ts 的 server.proxy 代理避免跨域。
VITE_API_BASE=/api
