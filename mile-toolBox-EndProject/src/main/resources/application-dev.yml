# 开发环境配置
# 真实连接信息（账号/密码/密钥）请放 application-local.yml（已 gitignore），不要写在本文件
spring:
  config:
    import: optional:classpath:application-local.yml

logging:
  level:
    com.example.miletoolboxendproject: debug
